# 项目进度 — 2026-09-19

## 当前结论

项目已经完成从仓库建模到空载路径物理执行的主链路。核心创新点
“根据高度与载货状态切换配置空间并规划路径”已经在离线规划层实现，
但完整的自动取货、载货运输和放货任务还没有闭环。

## 已完成并有证据

| 模块 | 状态 | 验证证据 |
|---|---|---|
| Unity 仓库与高低净空通道 | 完成 | 静态碰撞与通道采样通过 |
| 差速底盘与轮速反馈 | 完成 | WheelCollider 物理驱动、`/odom`、`/joint_states` |
| ROS 控制举升 | 完成基础闭环 | `/lift/command`、`/lift/state`、到位反馈 |
| 分层规划地图 | 完成 | Unity Collider 导出，三种模式、八个朝向 |
| Load-aware A* | 完成 | 10 个场景验证通过，独立几何复核通过 |
| Python 自动测试 | 完成 | 18 项测试通过 |
| 空载 Start→P1 动态执行 | 完成 | ROS 2 + Unity 物理闭环成功，终点误差约 0.0094 m |
| Unity C# 编译 | 完成 | Runtime/Editor 均为 0 warning、0 error |

## 尚未完成

- P1 货物的动态连接与载货状态确认；
- 载货状态下 P1→P2 的 Unity 物理执行；
- P2 下降、释放货物和任务完成确认；
- 一键执行完整任务的状态机；
- 二维雷达、临时障碍停车及在线重规划；
- 完整任务重复性测试、最终英文 README 和演示视频。

## 截止日前执行顺序

1. 首先完成货物 attach/detach 和载货状态，不再扩展场景美术。
2. 复用现有规划与跟踪器运行 loaded P1→P2。
3. 用最小顺序状态机串联空载导航、举升、载货导航、下降和释放。
4. 只在完整任务稳定后增加障碍检测；时间不足时降级为检测后停车。
5. 保存至少三次连续任务结果，更新 README 并录制演示。

## 范围说明

当前 A* 与几何验证不是完整动力学安全证明；空载联合测试已经经过 Unity
WheelCollider 物理执行。载货规划配置假设货物已经可靠连接，不能在实现
attach/detach 之前宣称完整取放任务完成。
